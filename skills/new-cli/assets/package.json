{
  "name": "my-cli",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "hud":   "bun src/cli.ts",
    "run":   "bun src/cli.ts run",
    "test":  "bun test",
    "mcp":   "bun src/mcp.ts",
    "serve": "bun src/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",

    "ink": "^5.0.0",
    "ink-text-input": "^6.0.0",
    "react": "^19.0.0",

    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.0.0"
  },

  "_comments": {
    "ink + react": "wizard-style CLI only — remove for HUD-style",
    "ink-text-input": "wizard-style only — open-ended text fields",
    "@modelcontextprotocol/sdk": "only if adding Claude Desktop MCP server",
    "scripts.mcp": "only if src/mcp.ts exists",
    "scripts.serve": "only if src/server.ts exists"
  }
}
