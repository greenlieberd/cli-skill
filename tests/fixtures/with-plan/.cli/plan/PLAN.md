# PLAN — with-plan-fixture

> Status: 0 of 5 tasks complete
> Planned: 2026-03-25

## v0.1 — ship this

- [ ] **Init project** `chore` — create folder, package.json, bun install, git init
- [ ] **HUD shell + ASCII logo** `feat` — src/hud.ts with logo, menu, resize handler
- [ ] **Configure + env** `feat` — src/configure.ts, .env.example
- [ ] **Tests** `test` — tests/cli.test.ts with bun:test
- [ ] **Verify** `chore` — bun hud starts, bun test passes

## v0.2+ — append after v0.1 ships

- [ ] **MCP server** `feat` — src/mcp.ts for Claude Desktop
