# PLAN

- Build a Claude Code plugin that makes spinning up a new Propane CLI a matter of hours, not days
- Capture every architecture decision in guides so they don't have to be re-learned
- Ship a `/new-cli` skill that asks 8 questions and outputs a working scaffold in one shot
- Cover the file→browser→CLI bridge so UIs are always optional, lightweight, and disposable
- Enforce the no-database rule: flat files + skills + agents replace all local data layers
